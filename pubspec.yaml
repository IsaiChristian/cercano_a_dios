name: cercano_a_dios
description: A private, offline Catholic prayer companion.
publish_to: none
version: 0.1.0+1
environment:
  sdk: '>=3.9.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_bloc: ^9.1.1
  provider: ^6.1.5+1
  go_router: ^17.2.3
  dartz: ^0.10.1
  sqflite: ^2.4.2
  path: ^1.9.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  sqflite_common_ffi: ^2.3.6
flutter:
  uses-material-design: true
  generate: true
